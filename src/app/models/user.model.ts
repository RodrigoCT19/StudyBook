export interface User {
    uid: string,
    email: string,
    password: string,
    name: string
}

export interface Users {
    name: string;
    email: string;
    code: string;
    cicle: string;
    career: string;
    password: string;
    disabled: boolean;
}